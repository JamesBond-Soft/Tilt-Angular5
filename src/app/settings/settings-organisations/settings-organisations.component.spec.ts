import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { HttpClientTestingModule, HttpTestingController } from '@angular/common/http/testing';
import { RouterTestingModule } from '@angular/router/testing';

import { SettingsOrganisationsComponent } from './settings-organisations.component';
import { SettingsOrganisationsService } from './settings-organisations.service';

describe('SettingsOrganisationsComponent', () => {
  // let component: SettingsOrganisationsComponent;
  // let fixture: ComponentFixture<SettingsOrganisationsComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     imports: [
  //       HttpClientTestingModule,
  //       RouterTestingModule
  //     ],
  //     declarations: [ SettingsOrganisationsComponent ],
  //     providers: [SettingsOrganisationsService  ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(SettingsOrganisationsComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
