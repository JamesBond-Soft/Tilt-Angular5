import { FilterSelfAssessmentReportPipe } from './filter-self-assessment-report.pipe';

describe('FilterSelfAssessmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelfAssessmentReportPipe();
    expect(pipe).toBeTruthy();
  });
});
