import { GenericStatusNamePipe } from './generic-status-name.pipe';

describe('GenericStatusNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GenericStatusNamePipe();
    expect(pipe).toBeTruthy();
  });
});
