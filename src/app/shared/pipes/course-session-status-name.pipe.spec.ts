import { CourseSessionStatusNamePipe } from './course-session-status-name.pipe';

describe('CourseSessionStatusNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseSessionStatusNamePipe();
    expect(pipe).toBeTruthy();
  });
});
