import { DataColumnFilterPipe } from './data-column-filter.pipe';

describe('DataColumnFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DataColumnFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
